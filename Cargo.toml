[package]
name = "minttp"
version = "0.1.0"
authors = ["jD91mZM2 <me@krake.one>"]
license = "MIT"

[dependencies]
native-tls = { version = "0.1", optional = true }
http       = { version = "0.1", optional = true }

[features]
default = ["native-tls"]
